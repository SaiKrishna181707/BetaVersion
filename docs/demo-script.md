# Centopus demo script

Show only capabilities supported by the configured deployment and evidence visible during recording. A local build or animation is not a live AWS run.

1. Introduce Centopus as synthetic usability testing for authorized products. Agents are not customers and do not measure demand.
2. Sign in as an operator. Show API configuration/status. If unavailable, describe the implementation rather than pretending a run is executing.
3. Create a run, review its planning estimate and acknowledge authorization. For Fieldwork, configure the relevant DOM checkpoints. Inspect and edit the seeded population.
4. Start with one user. Show persisted session states. The live dashboard polls status; video/Live View is not integrated. Animated dots are not browser evidence.
5. Inspect recorded actions, timestamps, failures and the explicit goal checkpoint after completion. Missing evidence stays missing; model prose does not prove success.
6. Show deterministic metrics and report evidence. Actual billing is unavailable; admission estimates are a separate quantity.
7. State configured limits: at most 100 users, five concurrent workers, 300 seconds and 40 actions per session. Do not call these a verified scale result without fresh AWS evidence.

No production URL is certified by this cleanup. Historical screenshots have the limitations documented in [verification status](final-deployment-report.md). Record a fresh Centopus demo with the tested commit and run ID.
