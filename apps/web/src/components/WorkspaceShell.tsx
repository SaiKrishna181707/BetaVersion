import type { ReactNode } from 'react';
import { GUARDRAILS, formatUsd } from '@synthetic-beta/contracts';
import { Badge, Brand, Icon } from '@synthetic-beta/ui';

export function WorkspaceShell({ children }: { children: ReactNode }) {
  return <div className="workspace-layout"><aside className="workspace-sidebar"><Brand /><div className="workspace-selector"><span className="workspace-avatar">S</span><div><strong>Sandbox workspace</strong><span>Local development</span></div></div><span className="nav-caption mono">WORKSPACE</span><nav aria-label="Workspace"><a href="#/"><Icon name="grid" />Overview</a><a className="active" href="#/new" aria-current="page"><Icon name="plus" />New run<span className="nav-active-dot" /></a></nav><div className="sidebar-empty"><Icon name="terminal" size={18} /><p>Your first session<br />starts with a clear goal.</p><span>Configure → review → run</span></div><div className="sidebar-budget"><div><Icon name="shield" size={14} /> GLOBAL CEILING</div><strong className="mono">{formatUsd(GUARDRAILS.GLOBAL_SPEND_CEILING_USD * 100)}<span>USD</span></strong><p>Configured limit · execution offline</p></div><div className="workspace-user"><span className="workspace-avatar small">L</span><span>Local workspace</span><Badge>DEV</Badge></div></aside><div className="workspace-content"><header className="workspace-topbar"><div className="mobile-brand"><Brand compact /></div><div className="breadcrumbs"><span>Workspace</span><Icon name="chevron" size={12} /><strong>New run</strong></div><Badge tone="accent"><span className="dot" />Local preview</Badge></header><main id="main" className="new-run-main">{children}</main><footer className="workspace-footer"><Icon name="lock" size={12} />Configuration stays in this browser. No agents are running.</footer></div></div>;
}
