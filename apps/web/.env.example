# Browser-visible configuration. Never put credentials or secrets in VITE_ variables.
# Exact extra hosts only. The deployed app automatically authorizes its own hostname so
# /demo-target/ can be used as the owned HTTPS hackathon target.
VITE_AUTHORIZED_DOMAINS=localhost,127.0.0.1
